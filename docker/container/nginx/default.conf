#server {
#    listen 80;
#    server_name localhost;
#    return 307 https://$host:18443$request_uri;
#}
server {
    server_name localhost;

    listen 80;
    listen 443 ssl;
    ssl_certificate /etc/nginx/ssl/server.crt;
    ssl_certificate_key /etc/nginx/ssl/server.key;

    #listen 80;
    client_max_body_size 10M;
    root /var/www/source/public;
    index index.php;
    location ~ /\.ht {
        return 404;
    }
    location / {
        root /var/www/source/public;
        index index.php;
        try_files $uri $uri/ /index.php?$query_string;
        # CORS start
        add_header Access-Control-Allow-Origin "http://localhost:3000" always;
        add_header Access-Control-Allow-Methods "POST, GET, OPTIONS" always;
        add_header Access-Control-Allow-Headers "Origin, X-Requested-With, Content-Type, Authorization, Accept, Credentials" always;
        add_header Access-Control-Expose-Headers "Authorization" always;
        add_header Access-Control-Allow-Credentials true always;
        # CORS end
    }
    location ~ \.php$ {
        try_files $uri =404;
        fastcgi_split_path_info ^(.+\.php)(/.+)$;
        fastcgi_pass app:9000;
        fastcgi_index index.php;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_param PATH_INFO $fastcgi_path_info;
        # CORS start
        add_header Access-Control-Allow-Origin "http://localhost:3000" always;
        add_header Access-Control-Allow-Methods "POST, GET, OPTIONS" always;
        add_header Access-Control-Allow-Headers "Origin, X-Requested-With, Content-Type, Authorization, Accept, Credentials" always;
        add_header Access-Control-Expose-Headers "Authorization" always;
        add_header Access-Control-Allow-Credentials true always;
        # CORS end
    }
}
server {
    listen 81;
    client_max_body_size 10M;
    root /var/www/xhprof/xhprof_html;
    index index.php;
    location ~ /\.ht {
        return 404;
    }
    location / {
        root /var/www/xhprof/xhprof_html;
        index index.php;
        try_files $uri $uri/ /index.php?$query_string;
        # CORS start
        add_header Access-Control-Allow-Origin "http://localhost:3000" always;
        add_header Access-Control-Allow-Methods "POST, GET, OPTIONS" always;
        add_header Access-Control-Allow-Headers "Origin, X-Requested-With, Content-Type, Authorization, Accept, Credentials" always;
        add_header Access-Control-Expose-Headers "Authorization" always;
        add_header Access-Control-Allow-Credentials true always;
        # CORS end
    }
    location ~ \.php$ {
        try_files $uri =404;
        fastcgi_split_path_info ^(.+\.php)(/.+)$;
        fastcgi_pass app:9000;
        fastcgi_index index.php;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_param PATH_INFO $fastcgi_path_info;
        fastcgi_param PHP_VALUE "auto_prepend_file=/var/www/xhprof/external/header.php";
        # CORS start
        add_header Access-Control-Allow-Origin "http://localhost:3000" always;
        add_header Access-Control-Allow-Methods "POST, GET, OPTIONS" always;
        add_header Access-Control-Allow-Headers "Origin, X-Requested-With, Content-Type, Authorization, Accept, Credentials" always;
        add_header Access-Control-Expose-Headers "Authorization" always;
        add_header Access-Control-Allow-Credentials true always;
        # CORS end
    }
}
